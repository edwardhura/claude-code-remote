# Context: chat-bot

## Files
- `src/ccr/bot/__init__.py` — empty package marker
- `src/ccr/bot/handlers/__init__.py` — empty package marker
- `src/ccr/bot/middlewares.py` — `AllowlistMiddleware`: extracts sender from Message/CallbackQuery, checks `auth.allowlist.is_paired`, injects `is_paired_user` into aiogram data, persists `last_chat_id` on paired senders, short-circuits non-`/start` traffic from unpaired senders (NOTE: pre-existing F1 advisory — unpaired callback-query tap does not call `cb.answer()`, leaving Telegram spinner hanging; track as separate ticket)
- `src/ccr/bot/notify.py` — `notify_owner` (queries owner row, sends to `last_chat_id`, warns if no owner/null chat_id); `broadcast_paired` (fans out to all paired users with non-null `last_chat_id`, skips `exclude` set, swallows per-recipient `TelegramAPIError`)
- `src/ccr/bot/handlers/pairing.py` — `/start` handler with three branches: paired status reply; bootstrap (no owner) reply with code + approve command; normal path reply + `notify_owner` with code. Note: username is interpolated unescaped in HTML mode (F1 advisory — harden with `html.escape()` when adding more HTML messages in CCR-008)
- `src/ccr/bot/keyboards.py` — `permission_kb(session_id, request_id, options) -> InlineKeyboardMarkup`: builds one button per option using `_LABELS` map for display text; unknown options fall back to `opt.capitalize()`; `callback_data=f"perm:{session_id}:{request_id}:{choice}"`; `ask_user_question_kb(tool_use_id, options) -> InlineKeyboardMarkup`: builds one button per option-index callback (under 64-byte cap) for AskUserQuestion prompts
- `src/ccr/bot/app.py` — `build_dispatcher(settings, db_factory, session_manager=None) -> tuple[Bot, Dispatcher]` (registers `AllowlistMiddleware`, stashes `db_factory`, `session_manager`, and `settings` in workflow data, includes pairing router, session router, permission router, `ask_user_question_router`, `config_router`, and passthrough router last); `run_polling` (logs `"Bot started, awaiting updates"` then enters `dp.start_polling(bot)`)
- `src/ccr/bot/handlers/config.py` — new module: `cmd_config` (`/config` command, opens menu or honours `/config tz <IANA name>` free-text fallback), `cb_open_tz_picker` (`cfg:tz` callback, renders curated 13-zone picker), `cb_pick_tz` (`cfg:tz:<idx>` callback, persists `_CURATED_ZONES[idx]` for caller), `cb_close` (`cfg:close` callback, edits to "Menu closed." drops reply_markup); `router` exported; all DB writes keyed on `cb.from_user.id`; validation via `zoneinfo.ZoneInfo`
- `alembic/versions/0002_add_paired_users_timezone.py` — additive migration adding nullable `timezone TEXT` to `paired_users`; reversible (downgrade drops column)
- `alembic/versions/0004_add_sessions_name.py` — additive migration adding nullable `name TEXT` to `sessions`; down-revision points at `0003_add_sessions_claude_session_id`; reversible (downgrade drops column)
- `src/ccr/db/models.py` — `PairedUser` gains `timezone: Mapped[str | None]` nullable IANA-zone field; NULL interpreted as UTC at render time; CCR-037: `Session` gains `name: Mapped[str | None]` nullable text field; NULL rows render `(unnamed)` in `/sessions`
- `src/ccr/bot/handlers/ask_user_question.py` (new) — AskUserQuestion router: button callbacks (option-index under `ask:` prefix), `/answer <id8> <text>` command with `_ID8_RE = ^[0-9a-zA-Z_-]{8}$`, and single-outstanding plain-text filter; validates `tool_use_id`, calls `session_manager.send_tool_result`; stale/unknown ids rejected with canned message; CCR-038: `_USAGE_HINT` now HTML-entity-escaped (`&lt;8-char-id&gt; &lt;text&gt;`); structlog logger added; `cmd_answer` usage-hint send wrapped in narrow `try/except TelegramBadRequest` with structlog warning
- `src/ccr/bot/formatting.py` — `chunk_text` and `event_to_messages(event: ClaudeEvent) -> list[OutboundMessage]` where `OutboundMessage = tuple[str, InlineKeyboardMarkup | _PendingKeyboard | None]`; `PermissionRequest` branch returns a `_PendingKeyboard` sentinel (pure function cannot access session_id); `_PendingKeyboard.kind` discriminator distinguishes `"permission"` vs `"ask_user_question"` sentinels; `_format_ask_user_question` renders nested-questions schema (each question's text + options); result line: `✅ done · {s}s · {N}k tokens`; all user-controlled strings escaped via `html.escape()`; CCR-028 added `_AUQ_SUPPRESSED_TOOL_NAMES = frozenset({"AskUserQuestion"})` defensive guard — `_format_mcp_permission` returns `None` for suppressed tool names, `event_to_messages` drops the event silently (defence in depth: the bus suppression at MCP source is the primary guard; formatter guard handles schema drift / test fixtures)
- `src/ccr/claude/mcp.py` — (cross-listed from claude-runtime) CCR-028 extended `McpPermissionServer.__init__` with `suppressed_tool_names: frozenset[str] = frozenset()` and `set_suppressed_tool_handler(cb)` registration; `_on_tool_call` branches on suppressed names — skips bus publish and invokes the registered handler with `(request_id, tool_name, tool_input)` instead; resolution still flows through existing `resolve(request_id, decision)` path; defensive fallback: if no handler registered, suppressed name falls through to legacy bus-publish
- `src/ccr/bot/handlers/session.py` — `/new`, `/stop`, `/clear`, `/who`, `/pid`, `/sessions`, `/continue`, `/rename` command handlers plus `handle_text` plain-text handler (filter narrowed to `F.text & ~F.text.startswith("/")` so slash commands fall through to passthrough router); `/new` and `/clear` replies include `(pid <N>)`; `/clear` also broadcasts a `_DIVIDER_MESSAGE` ("— — — new session — — —") to all paired chats via `broadcast_paired` between `manager.stop()` and `manager.new_session()`, gated on `prior_status != IDLE` (read before stop) so an idle `/clear` does not post a misleading divider; `/pid` shows session id, pid, and uptime; "No active session." covers both no-subprocess and subprocess-but-pre-SystemInit cases after CCR-044; `/sessions` joins on `paired_users` to fetch tg_username for each starter; CCR-044: `cmd_sessions` adds `Session.status != SessionStatus.IDLE.value` to the SQL `.where()` clause so idle rows never reach `_format_session_row` and never consume the 20-row budget; CCR-042 (fix-loop): `_format_session_row(row, user, usernames)` renders the session-id slot as `claude_session_id[:8]` (HTML-escaped) when set; renders `<code>--------</code>` (module constant `_NULL_CLAUDE_SESSION_ID_MARKER`) when `claude_session_id IS NULL` — legacy-only artefact after CCR-044, constant and rendering preserved; `cmd_sessions` docstring updated to document the marker as legacy-only; `cmd_pid` docstring updated to note "No active session." covers both no-subprocess and pre-SystemInit cases; `cmd_sessions` docstring documents the marker, the alignment invariant, and the `<id8> · <status> · <name> · HH:MM DD-MM · by @<username>` format; `_format_started_by` is the trailing `by …` helper; CCR-043: `cmd_rename` rekeyed from `str(row.id)[:8]` to `claude_session_id[:8]` prefix matching; rows with `claude_session_id IS NULL` are skipped from prefix matching; entire resume chain renamed in one commit; new `/rename current <name>` branch via `_handle_rename_current` helper that reads `SessionManager.info()`; `_RENAME_NO_ACTIVE_SESSION_REPLY` constant (no subprocess held); `_RENAME_IDLE_ACTIVE_SESSION_REPLY` constant (subprocess held but `claude_session_id` not yet populated — CCR-044 `[idle]` window); `_RENAME_USAGE_HINT` updated to document both forms with HTML-entity-escaped `&lt;` / `&gt;`; handler signature now takes `session_manager: SessionManager` (injected via `dp["session_manager"]`); `__all__` exports `cmd_rename`; `_UNNAMED_PLACEHOLDER = "(unnamed)"`, `_SESSION_NAME_MAX_LEN = 40`; empty DB returns stable `"(no sessions)"` string; body capped at 3500 chars via `chunk_text`; `/continue` calls `manager.continue_session()`, replies `"Session <id8> resumed (pid <N>)."`, catches `SessionAlreadyRunningError` and `NoPriorSessionError` with canned strings, catches bare `SessionError` with HTML-escaped message; old `_FIRST_PROMPT_DISPLAY_TRUNCATE` constant removed
- `src/ccr/bot/handlers/passthrough.py` — catch-all slash-command router registered last in `app.py`; `WHITELIST = frozenset({"model", "compact"})` forwarded via `send_slash` (`"cost"` lifted out in CCR-023); `BLOCKED_INTERACTIVE = {"mcp", "init"}` returns canned local-terminal redirect; `/agents` handled by dedicated early-return branch rendering Running (live subagents from `SessionManager.running_subagents()`), Project agents (`.claude/agents/*.md` glob with per-file frontmatter model), and Built-in agents (`BUILTIN_AGENTS` constant) sections in HTML in fixed order Running → Project agents → Built-in agents; project-agent lines render as `<name> · <model>` with HTML-escaping; `/skills` branch renders a single "Skills" section from `SessionManager.available_skills()` with HTML-escaped names and `"(none)"` empty-state placeholder; `/cost` handled by dedicated `_reply_cost` branch that calls `session_manager.current_session_usage()` and renders an HTML reply with tokens (input/output/cache), tool-use count, elapsed time, session id8, and optionally cost in USD (omitted for subscription users when `total_cost_usd == 0`); `/usage` handled by dedicated `_reply_usage` branch (CCR-032) that calls `session_manager.current_rate_limit_status()` and renders an HTML reply with rate-limit window, used/limit counts, reset times, and `is_using_overage` flag; three reply states: no active session / no rate-limit data yet / full render; returns `"No active session."` when idle; unknown commands return the stable usage hint listing all whitelisted commands including future Phase 13 entries; CCR-034: `/config` appended to `_UNKNOWN_USAGE_HINT`; CCR-037: `/rename` appended to `_UNKNOWN_USAGE_HINT`; CCR-039: added `RATE_LIMIT_WINDOW_LABELS` and `OVERAGE_REASON_LABELS` constant maps; `_humanise_code(code, label_map)` helper (mapped label or title-case-with-spaces fallback); `_format_resets_at` / `_render_usage_reply` widened to accept `PairedUser | None` and thread it through `format_user_datetime("full")`; `_reply_usage` now looks up the calling PairedUser via `_load_paired_user(db_factory, tg_user_id)`; `cmd_passthrough` gains optional `db_factory=None` kwarg (injected by aiogram from `dp["db_factory"]` in production); new import edge: `ccr.db.models.PairedUser` and `sqlalchemy.select`; CCR-040: added `BUILTIN_AGENTS: dict[str, str]` constant (CLI v2.1.131, version comment required above declaration); added `_parse_frontmatter_model(path)` stdlib-only frontmatter parser (falls back to `"inherit"` on missing/malformed block); `_list_library_agents` now returns `list[tuple[str, str]]` of `(name, model)` pairs; `_render_agents_reply` now takes `(running, project_agents, builtin_agents)` and emits three sections in the fixed order; "Library" section renamed to "Project agents"
- `src/ccr/bot/handlers/permission.py` — `cb_permission` callback-query handler matching `F.data.startswith("perm:")`; parses `perm:{session_id}:{request_id}:{choice}`; validates choice against `_pending_options` frozenset; handles stale session, stale request, forged choice, malformed data, concurrent taps (via `contextlib.suppress(TelegramBadRequest)`), missing username, edit failure; edits message to remove keyboard and append `→ {choice} (by @{username})` with HTML escaping
- `src/ccr/bot/typing.py` — `TypingKeepalive` class: per-chat task sending `send_chat_action("typing")` every 4s while a session is producing events; swallows `TelegramAPIError`; lifecycle mirrors `_ChatSender` (`start`/`cancel`/`wait_closed`)
- `src/ccr/server.py` — `async serve(settings)` runs bot polling, `_broadcast_loop`, and `_typing_loop` concurrently; `_ChatSender` per-chat queue; `_broadcast_loop` takes `manager: SessionManager`, materialises `_PendingKeyboard` sentinels via `_materialise_keyboards` (dispatches on `kind` — `"permission"` builds `permission_kb`, `"ask_user_question"` builds `ask_user_question_kb`), special-cases `PermissionRequest` (flush buffer + send immediately), buffers non-permission events while gate is paused, drains buffer on gate reopen; `_ChatSender._run` unpacks `OutboundMessage` tuple and passes `reply_markup` to `bot.send_message`; `TypingKeepalive` tasks started/cancelled per session event stream
- `src/ccr/cli.py` — `_cmd_serve` wires `serve` subcommand to `asyncio.run(server.serve(Settings()))`
- `src/ccr/config.py` — (cross-listed from core) CCR-026 added `ask_user_question_timeout_seconds: int = 600` setting controlling how long the bot waits for a user reply before timing out an AskUserQuestion prompt
- `tests/test_bot_ask_user_question.py` (new) — 35 tests covering AskUserQuestion broadcast (nested-questions schema), button callback path, `/answer <id8> <text>` command path, plain-text single-outstanding filter, stale/unknown id rejection, timeout behaviour, teardown on session stop, regression test `test_cmd_answer_with_real_format_tool_use_id` (toulu_ prefix), and malformed-args length+charset cases; CCR-038: three existing usage-hint assertions retargeted to entity-escaped string; two new regression tests added (`test_cmd_answer_no_args_renders_html_escaped_usage_hint`, `test_cmd_answer_usage_hint_swallows_telegram_bad_request`); 38 total tests
- `tests/test_bot_session.py` — extended with four `cmd_sessions` tests (empty DB, 3-row ordering, long-prompt truncation, status passthrough) plus a `_seed_session` helper; CCR-042: updated `test_cmd_sessions_renders_five_field_format_with_claude_session_id_and_username` to assert 8-char prefix and that the full UUID does NOT appear; added `test_cmd_sessions_resume_chain_rows_render_same_claude_id_prefix`
- `tests/test_bot_session_handlers.py` — (new, CCR-044) two tests: `test_sessions_filters_idle_rows` (idle row absent from listing), `test_sessions_still_renders_legacy_null_marker_for_non_idle_rows` (legacy NULL-`claude_session_id` row with terminal status renders `--------` marker)
- `tests/test_orphan_reconcile.py` — new file with four tests covering reconcile contract (running→crashed, exit_reason, ended_at populated, second call is no-op)
- `tests/test_bot_pairing.py` — 7 tests covering all four acceptance paths: bootstrap, normal, already-paired, unpaired plain text rejection, plus middleware pass-through scenarios
- `tests/test_bot_permission.py` — 17 tests covering keyboard shape, `_parse_callback`, every handler edge case (stale id, stale request, forged choice, malformed data, concurrent taps, missing username, edit failure, HTML escape)
- `tests/test_formatting.py` — updated for new `OutboundMessage` tuple shape; includes `test_permission_request_returns_message_with_keyboard_sentinel` and `test_permission_request_html_escapes_tool_name_and_input`
- `tests/test_broadcast.py` — includes `test_broadcast_permission_message_carries_keyboard`, `test_broadcast_buffers_text_event_during_permission_then_drains_in_order`, `test_broadcast_sse_subscriber_not_paused`; extended in CCR-026 with AskUserQuestion broadcast cases
- `tests/test_typing.py` — 8 tests covering `TypingKeepalive` firing, looping, error swallowing, idempotent cancel/start, and `_typing_loop` integration
- `tests/test_bot_continue.py` — 7 tests: happy-path reply, already-running canned string, no-prior canned string, other `SessionError` HTML-escaped, valid 8-hex prefix forwards `session_id_prefix` kwarg, invalid-format rejected without manager call (5 inputs), unknown prefix returns not-found canned string
- `tests/test_bot_passthrough.py` — renamed from `tests/test_passthrough.py` (CCR-023); extended with `/skills` tests (active session with skills, empty skills array, no active session, HTML escaping of skill names, `/skills` removed from unknown-command fallthrough); extended with 7 new CCR-023 `/cost` tests (rich HTML reply, no-active-session canned string, cost-line omission for subscription users, HTML-escape on session id, multi-minute elapsed format, `/model`+`/compact` whitelist regressions, explicit `send_slash` non-call assertion); previously 15 tests covering `/agents` Running+Library rendering, empty-state placeholders, HTML escaping, alphabetical sort, non-md/hidden file skipping, `/mcp` and `/init` regression, and unknown-command hint; CCR-034: bumped hint assertion to include `/config`
- `tests/test_bot_config.py` — **new (CCR-034)**. 16 tests covering: menu opens with Timezone+Close buttons, timezone picker renders curated zones, picker button persists valid zone to `paired_users` for caller, free-text `/config tz` persists valid IANA name, invalid IANA name rejected (no DB write), Close button dismisses menu cleanly; all six acceptance criteria covered.
- `tests/test_session_manager.py` — extended with two CCR-022 tests: `test_running_subagents_tracks_agent_tool_use_and_clears_on_result`, `test_running_subagents_ignores_non_subagent_tools`; extended in CCR-030 with tests for `available_skills()` and `is_session_active()` accessors; extended in CCR-023 with three tests for `current_session_usage()`: idle returns None, live session aggregates JSONL via fake_claude, returns None after `stop()`; CCR-037: four new tests covering auto-fill happy path, long-prompt truncation, NULL-prompt path, and WHERE-name-IS-NULL invariant (manual-rename-sticks rule); CCR-042: updated `test_continue_session_with_prefix_resumes_matched_row` and `test_continue_session_lookup_by_prefix_excludes_crashed` to use `claude_session_id`-derived prefixes; renamed/rewrote `test_continue_session_by_prefix_skips_null_claude_session_id_rows` (formerly raises `NoPriorSessionError`, now asserts `SessionNotFoundError`); added four new tests: `test_lookup_by_prefix_matches_claude_session_id_first_8`, `test_lookup_by_prefix_unknown_raises_session_not_found`, `test_lookup_by_prefix_only_null_rows_raises_not_found`, `test_lookup_by_prefix_resume_chain_returns_most_recent`
- `tests/test_bot_passthrough.py` — CCR-037: one-line update to `test_unknown_command_returns_usage` to include `/rename` in the expected hint string
- `src/ccr/claude/manager.py` — (cross-listed from claude-runtime) CCR-030 added `available_skills() -> list[str]` (reads `_skills` snapshot from most-recent `SystemInit` event) and `is_session_active() -> bool` accessors; `_skills: list[str]` instance attribute tracked on `SystemInit` events; CCR-023 added `current_session_usage() -> SessionUsage | None` accessor that resolves the live session's JSONL path and delegates to `aggregate_session_usage`; returns `None` when idle; CCR-032 added `_rate_limit_status: RateLimitEvent | None` instance attribute populated in `_consume_events` and reset at all three session boundaries (`new_session`, `continue_session`, `_teardown_locked`); `current_rate_limit_status() -> RateLimitEvent | None` synchronous accessor; CCR-026 added `_PendingQuestion` dataclass + `_pending_questions: dict[str, _PendingQuestion]`, `async send_tool_result(tool_use_id, content, *, is_error=False) -> None`, timeout tasks (asyncio.Task per pending question), and teardown cancellation of outstanding question tasks; CCR-028 extended `_PendingQuestion` with `mcp_request_id: str | None`, added `_unpaired_mcp_auq_calls: deque[str]` for MCP-first race, added `_on_mcp_ask_user_question` handler (FIFO-pairs incoming MCP request_id with `_pending_questions` entries), extended `_track_ask_user_question` to drain the deque on registration, and branched `send_tool_result` / `_handle_question_timeout` to resolve MCP Futures via `deny + message` for MCP-paired entries (claude's harness writes the synthetic tool_result) rather than calling `_deliver_tool_result` directly; CCR-037 added module-level `_SESSION_NAME_MAX_LEN = 40`, `_truncate_session_name(text)` helper (collapse whitespace, hard-cut + U+2026 ellipsis when over cap), `SessionManager._auto_fill_session_name(session_id, prompt)` UPDATE-WHERE-NULL helper, and `_db_insert_session` now calls the helper after row insert when a prompt is given; CCR-042: `_db_lookup_resumable_claude_session_id` prefix-match arm rekeyed to compare `row.claude_session_id[:8] == session_id_prefix` (string slice — stored as `str` per CCR-036, not `uuid.UUID`); rows with `claude_session_id IS NULL` are skipped from prefix matching entirely; `started_at desc → first` ordering preserved for multi-row resume-chain matches; no-prefix path unchanged
- `src/ccr/claude/events.py` — (cross-listed from claude-runtime) CCR-030 added `skills: list[str]` field to `SystemInit` event model to surface the `"skills"` array from the `system/init` JSONL event; CCR-032 added `RateLimitInfo` (camelCase aliases via `populate_by_name=True`, all fields default `None`, `extra="allow"`) and `RateLimitEvent` Pydantic models; both added to `_KnownEvent` discriminated union, `ClaudeEvent` type alias, and `__all__`
- `src/ccr/claude/usage.py` — **new (CCR-023)**. `SessionUsage` frozen dataclass + `aggregate_session_usage(jsonl_path)` walker; private `_Accumulator` + helpers `_absorb_result` / `_absorb_assistant_turn`. Aggregates input/output tokens, cache tokens, tool-use count, success-turn count, total elapsed (with `duration_api_ms` fallback), and total cost USD across every `result` event in the JSONL. Forward-compatible: `UnknownEvent` lines and parse errors fall through silently.
- `tests/test_claude_usage.py` — **new (CCR-023)**. Ten unit tests for `aggregate_session_usage`: missing/empty file, single result, multi-result accumulation, tool_use counting, text-only ignored, parse-error fallthrough, no-usage result, `duration_api_ms` fallback, error-subtype not counted as turn.

- `src/ccr/utils.py` — new module exposing `format_user_datetime(dt, user, mode) -> str`; single source of truth for bot-side timestamp rendering; defensively never raises on bad zone (logs structlog warning + UTC fallback); treats naive datetime as UTC.
- `tests/test_utils_datetime.py` — 13 unit tests for `format_user_datetime` (all three modes, English month abbreviations + no-leading-zero day in `"short"` mode, timezone shift, UTC fallback, unresolvable zone + warning, DST edge, naive datetime).
- `tests/test_bot_session.py` — extended with two regression tests pinning helper-driven `/sessions` rendering (UTC fallback + Asia/Tokyo shift); `_format_session_row` now takes `PairedUser | None`; CCR-037: `_seed_session` helper extended with `name` and `claude_session_id` kwargs; 13 new tests covering five-field format, `claude_session_id` fallback, username/tg_user_id fallbacks, HTML escape regression, and full `/rename` command surface; `test_cmd_sessions_long_prompt_truncated_to_60_chars` replaced by `test_cmd_sessions_renders_unnamed_placeholder_when_name_is_null`; 36 total tests in this file.
- `tests/test_bot_passthrough.py` — `test_usage_resets_at_in_the_past` updated to expect `HH:MM - DD/MM/YYYY` format from the helper on the "Resets at" line; CCR-039: added `usage_engine` / `usage_session_factory` async fixtures and `_seed_paired_user` helper; 8 new test cases: parametrised window/overage label tests, two unknown-code fallback tests, two timezone tests (non-UTC shift + UTC fallback), one `(in {delta})` regression test; two existing tests updated to assert humanised labels and title-case-then-escape pipeline; CCR-040: renamed `<b>Library</b>` assertions to `<b>Project agents</b>`, added `· inherit` model-column assertions on existing fixtures, added `_seed_agent_with_frontmatter` helper, added 9 new tests (model-opus, no-model-key, no-frontmatter, malformed-frontmatter, builtin-section-order, full section-order, name-with-metachars escape, builtin-value escape, version-comment presence); 53 tests total in this file

## Relations
- depends on: auth, core, claude-runtime

## Change history
- [CCR-006]: bot scaffold + allowlist middleware + /start pairing flow + serve() polling entrypoint
- [CCR-008]: session lifecycle handlers (/new /stop /clear /who + plain-text forwarding), ClaudeEvent → Telegram HTML formatter with chunking, per-chat queue broadcast loop wired into serve()
- [CCR-018]: PID exposure via /new, /clear, /pid; result line rewritten (no $cost, seconds-format duration, optional token segment); new typing.py keepalive module wired as _typing_loop in serve()
- [CCR-009]: permission inline-button handling end-to-end — keyboards.py, handlers/permission.py (cb_permission), formatting.py OutboundMessage tuple + _PendingKeyboard sentinel, manager.py gating dicts (is_telegram_paused, wait_for_resume, _clear_pending_permission), server.py _broadcast_loop with buffer drain, permission router included in app.py; 17 new tests in test_bot_permission.py, 3 new in test_broadcast.py, 4 new in test_session_manager.py
- [CCR-010]: slash-command passthrough whitelist — new handlers/passthrough.py with three-branch dispatch (whitelist → send_slash, blocked-interactive → canned reply, unknown → usage hint); manager.py gains send_slash wrapper; session.py handle_text filter narrowed to exclude slash commands; passthrough_router registered last in app.py; 6 tests in test_passthrough.py (100% coverage on passthrough.py); 209 total tests, 88.64% coverage
- [CCR-019]: /sessions command + orphan reconciliation — cmd_sessions handler added to session.py (20-row DESC listing, HTML-escaped, chunk_text capped); SessionManager.reconcile_orphans() added (idempotent, sets crashed/exit_reason/ended_at, logs per row); server.py awaits reconcile_orphans() before broadcast/typing tasks; new tests/test_orphan_reconcile.py (4 tests); tests/test_bot_session.py extended (4 new tests); 217 total tests, 88.81% coverage
- [CCR-020]: /continue command — ClaudeProcess gains `resume: bool | str = False` kw-only param (Outcome 1: `--continue` flag confirmed working); SessionManager gains `continue_session(*, started_by_tg_user_id, session_id_prefix=None)`, `SessionAlreadyRunningError`, `NoPriorSessionError`, `SessionNotFoundError`, `_RESUMABLE_STATUSES`, `_db_lookup_most_recent_finished()`, `_db_lookup_session_by_prefix()`; cmd_continue handler parses optional `^[0-9a-f]{8}$` prefix arg via module-level `_HEX8_RE`, rejects invalid formats without calling manager, catches `SessionNotFoundError` before bare `SessionError`; fake_claude.py gains FAKE_CLAUDE_ARGV_FILE directive; new tests/test_bot_continue.py (7 tests); test_session_manager.py extended (9 tests); 233 total tests, 88.90% coverage
- [CCR-022]: extended /agents to render Running (live subagents from SessionManager) + Library (.claude/agents/*.md) sections; added SessionManager.running_subagents() accessor and _track_subagents() helper tracking Task/Agent ToolUseBlock/ToolResultBlock events; injected Settings into bot DI via dp["settings"]; removed "agents" from BLOCKED_INTERACTIVE (now {"mcp", "init"}); 12 new tests in test_passthrough.py and test_session_manager.py; 270 total tests, 87.64% coverage
- [CCR-030]: Added /skills command listing skills from system/init events; SystemInit gained a skills field; SessionManager gained available_skills() and is_session_active() accessors.
- [CCR-031]: Extended /clear to broadcast a "— — — new session — — —" divider to all paired chats between stop() and new_session() via broadcast_paired, gated on prior_status != IDLE so idle clears stay quiet; reuses notify.broadcast_paired's per-recipient TelegramAPIError swallow; no new module / abstraction / DB schema; tests/test_bot_session.py extended with four /clear cases (paired-broadcast, idle-skip, swallow-failures, no-paired-users).
- [CCR-023]: Lifted /cost out of WHITELIST into a dedicated _reply_cost branch in passthrough.py; introduced src/ccr/claude/usage.py (SessionUsage dataclass + aggregate_session_usage JSONL walker); added current_session_usage() accessor to SessionManager; /cost now replies with a rich HTML summary (tokens in/out/cache, tools used, elapsed, session id8, optional cost USD); subscription users (total_cost_usd == 0) omit the cost line. Tests/test_bot_passthrough.py renamed from tests/test_passthrough.py; 27 tests total; 10 new tests in tests/test_claude_usage.py; 3 new tests in tests/test_session_manager.py; 298 total tests, 88.50% coverage.
- [CCR-032]: /usage command — local render of rate_limit_event snapshot via new RateLimitEvent model + SessionManager._rate_limit_status slot + passthrough _reply_usage branch. Probe (Outcome 1+2 hybrid) confirmed claude -p recognises /usage but short-circuits it; implementation uses out-of-band rate_limit_event JSONL lines as substrate. RateLimitInfo/RateLimitEvent added to events.py discriminated union; _rate_limit_status reset at all three session boundaries; _reply_usage renders window/used/limit/reset/overage fields in HTML with full escaping. 9 new /usage tests in test_bot_passthrough.py, 5 new in test_claude_events.py, 4 new in test_session_manager.py; 316 total tests, 88.65% coverage.
- [CCR-026]: AskUserQuestion handler — bot surfaces nested-questions tool_use blocks; replies via button tap, /answer <id8>, or single-outstanding plain text feed back as ToolResultBlock via SessionManager.send_tool_result. Step-0 probe revealed nested `questions` schema (array of {question, options} inside tool_input); _PendingQuestion + _pending_questions dict added to manager.py with timeout tasks and teardown cancellation; _PendingKeyboard.kind discriminator added to formatting.py; _materialise_keyboards in server.py dispatches on kind; ask_user_question_kb added to keyboards.py; config.ask_user_question_timeout_seconds = 600; ask_user_question_router registered in app.py; _ID8_RE = ^[0-9a-zA-Z_-]{8}$ (broadened from _HEX8_RE to accept toulu_ real-world prefixes); 35 tests in test_bot_ask_user_question.py; 369 total tests, 88.76% coverage.
- [CCR-028]: AUQ/MCP permission gate collision fix — McpPermissionServer gains suppressed_tool_names frozenset + set_suppressed_tool_handler callback; _on_tool_call skips bus publish for AskUserQuestion and routes to SessionManager._on_mcp_ask_user_question instead; _PendingQuestion gains mcp_request_id field; send_tool_result and _handle_question_timeout resolve MCP Futures via deny+message (harness writes synthetic tool_result, eliminating double-tool_result race); _unpaired_mcp_auq_calls deque handles MCP-first race ordering; formatting.py adds defensive _AUQ_SUPPRESSED_TOOL_NAMES guard returning [] for suppressed tools; 16 new tests across test_mcp_tool.py, test_session_manager.py, test_bot_ask_user_question.py, test_formatting.py; 385 total tests, 88.80% coverage.
- [CCR-034]: /config command + per-user timezone preference — new handlers/config.py with cfg:* router (cmd_config, cb_open_tz_picker, cb_pick_tz, cb_close); curated 13-zone picker + free-text /config tz <IANA name> fallback; validation via zoneinfo.ZoneInfo; DB keyed on cb.from_user.id (never callback payload); config_router registered between permission and passthrough routers in app.py; alembic migration 0002 adds nullable timezone TEXT to paired_users; PairedUser.timezone Mapped[str|None] in models.py; /config appended to _UNKNOWN_USAGE_HINT; 16 new tests in test_bot_config.py; test_bot_passthrough.py updated for hint; 401 total tests, 89.03% coverage.
- [CCR-035]: format_user_datetime helper + bot-wide datetime standardisation — new src/ccr/utils.py exposing format_user_datetime(dt, user, mode) with modes full (`HH:MM - DD/MM/YYYY`), short (`HH:MM - D Mon` with locale-independent English month abbreviation and no leading zero on day), time (`HH:MM`); zoneinfo-based timezone resolution, defensive UTC fallback + structlog warning on unresolvable zone, naive-datetime-as-UTC handling; swept all strftime call sites in src/ccr/bot/ (two converted: _format_session_row in session.py now passes PairedUser | None to helper in "short" mode; _format_resets_at in passthrough.py routes through helper with user=None pending CCR-039); python-developer.md agent rule added; 13 new tests in test_utils_datetime.py, 2 regression tests in test_bot_session.py, test_bot_passthrough.py updated.
- [CCR-037]: Added Session.name column + Alembic migration (0004_add_sessions_name.py); SessionManager auto-fills name from first prompt (Python-side NULL guard after SELECT, equivalent to WHERE-NULL under the single-session invariant); /sessions reformatted to `<claude_session_id> · <status> · <name> · HH:MM - D Mon · by @<username>`; /rename command added (manual rename always wins); _UNKNOWN_USAGE_HINT now lists /rename; _RENAME_USAGE_HINT entity-escapes &lt;,&gt; for HTML parse mode (F1 fix: literal `<`/`>` caused TelegramBadRequest); regression test test_cmd_rename_usage_hint_is_html_safe added; 462 total tests, 88.91% coverage.
- [CCR-038]: Bugfix: `_USAGE_HINT` in `ask_user_question.py` now HTML-entity-escaped (`&lt;8-char-id&gt; &lt;text&gt;`) fixing TelegramBadRequest on `/answer` with no args; structlog logger added; `cmd_answer` usage-hint send wrapped in narrow `try/except TelegramBadRequest` with structlog warning; audit confirmed no other raw `<placeholder>` strings in `src/ccr/bot/`; three existing tests retargeted to escaped string; two new regression tests added; 464 total tests, 88.92% coverage.
- [CCR-039]: `/usage` cosmetics — added `RATE_LIMIT_WINDOW_LABELS` / `OVERAGE_REASON_LABELS` constant maps and `_humanise_code` helper to `passthrough.py`; widened `_format_resets_at` / `_render_usage_reply` / `_reply_usage` / `cmd_passthrough` to accept `PairedUser | None` and thread it through `format_user_datetime("full")`; new `_load_paired_user` helper fetches caller from DB via `db_factory`; `cmd_passthrough` gains optional `db_factory=None` kwarg (production always injected from `dp["db_factory"]`); new import edge passthrough → `ccr.db.models.PairedUser`; 8 new + 2 updated tests in `test_bot_passthrough.py`; 472 total tests, 89.10% coverage.
- [CCR-040]: `/agents` reply extended to mirror Claude Code CLI library view — added `BUILTIN_AGENTS: dict[str, str]` constant (CLI v2.1.131 with version comment); added `_parse_frontmatter_model` stdlib-only frontmatter parser (falls back to `"inherit"` on missing/malformed/absent block); `_list_library_agents` now returns `list[tuple[str, str]]` of `(name, model)` pairs; `_render_agents_reply` now emits three sections in fixed order Running → Project agents → Built-in agents with HTML-escaped `<name> · <model>` lines; "Library" heading renamed to "Project agents"; 9 new + 4 updated tests in `test_bot_passthrough.py`; 481 total tests, 89.16% coverage.
- [CCR-042]: `/sessions` and `/continue` rekeyed from local `Session.id` UUID prefix onto `claude_session_id[:8]`; NULL rows now render the `--------` marker (8 ASCII dashes, new `_NULL_CLAUDE_SESSION_ID_MARKER` constant) instead of the old local-UUID fallback — eliminates the listing/continue misalignment; `_format_session_row` renders `claude_session_id[:8]` (HTML-escaped) when set, `<code>--------</code>` when NULL; `_db_lookup_resumable_claude_session_id` prefix arm compares `row.claude_session_id[:8]`; NULL rows skipped from prefix matching (`SessionNotFoundError` raised); `started_at desc → first` ordering preserved; no-prefix path unchanged; alignment-invariant test added asserting every 8-hex prefix in the listing is resolvable; 488 total tests, 89.10% coverage.
- [CCR-044]: `cmd_sessions` adds SQL-layer `Session.status != IDLE` filter (idle rows filtered before the 20-row limit, never reach `_format_session_row`); `_NULL_CLAUDE_SESSION_ID_MARKER` marked legacy-only (comment-only, constant and rendering preserved for pre-CCR-044 NULL rows); `cmd_pid` docstring updated to note "No active session." covers both no-subprocess and pre-SystemInit cases; new test file `tests/test_bot_session_handlers.py` with two tests; 497 total tests, 89.19% coverage.
- [CCR-043]: `cmd_rename` rekeyed from `str(row.id)[:8]` to `claude_session_id[:8]` prefix matching; NULL-`claude_session_id` rows skipped from prefix matching (short-circuit None-check before slice); entire resume chain renamed in one commit (WHERE `claude_session_id == <value>`); new `/rename current <name>` branch via `_handle_rename_current` helper reading `SessionManager.info()`; two distinct error constants (`_RENAME_NO_ACTIVE_SESSION_REPLY`, `_RENAME_IDLE_ACTIVE_SESSION_REPLY`) guaranteed different; `_RENAME_USAGE_HINT` updated to document both forms with HTML-entity-escaped angle brackets; handler now injects `session_manager` from `dp["session_manager"]`; 8 new + updated tests in `tests/test_bot_session.py`; 504 total tests, 89.32% coverage.
