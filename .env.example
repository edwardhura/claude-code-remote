# TODO(CCR-002): replace this minimal template with the full Section 7 template.
# This stub only carries the keys CCR-003's `Settings` requires so that
# `cp .env.example .env && python -m ccr init-db` works.

TELEGRAM_BOT_TOKEN=123456:ABCdef
PUBLIC_URL=https://example.com
JWT_SECRET=
DATA_DIR=./data
