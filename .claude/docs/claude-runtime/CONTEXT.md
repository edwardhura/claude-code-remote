# Context: claude-runtime

## Files
- src/ccr/events/__init__.py — re-exports `EventBus` and `Subscriber`
- src/ccr/events/bus.py — `EventBus` with WeakSet-tracked subscriber queues, snapshot-iterating publish, drop-oldest backpressure with WARN, async-generator `subscribe()` with explicit `finally` discard
- src/ccr/claude/__init__.py — public surface re-exports for the claude subpackage (includes `McpPermissionServer`, `McpPermissionRequest` since CCR-025)
- src/ccr/claude/state.py — `SessionStatus(StrEnum)` with five states: IDLE, RUNNING, COMPLETED, STOPPED, CRASHED
- src/ccr/claude/events.py — Pydantic v2 schema; `ClaudeEvent` plain Union over `_KnownEvent` discriminated union + `UnknownEvent`; `parse_event()` with double-fallback; `ContentBlock` inner/outer variants (text, thinking, tool_use, tool_result); `ResultUsage` sub-model with optional `usage` field on `ResultEvent`; `PermissionRequest` variant removed (CCR-024); `McpPermissionRequest` synthetic bus envelope added (CCR-025, NOT a ClaudeEvent member)
- src/ccr/claude/mcp.py — `McpPermissionServer`: in-process MCP server exposing `ccr_permission_prompt` tool; `request_id → asyncio.Future` map; Unix-socket listener; deterministic `.ccr-mcp-config.json` config file; `start()`/`stop()` lifecycle; `resolve()`/`cancel_pending()`/`is_pending()` API; `claude_argv` property; `_handle_relay_connection` fully implemented as a relay bridge (CCR-028): frames newline-delimited JSON-RPC bytes from the Unix socket into `SessionMessage` objects, runs three concurrent tasks (socket reader, socket writer, `Server.run`) under `anyio.create_task_group`; `os.chmod(sock_path, 0o700)` applied after socket creation; module-level `_socket_reader`/`_socket_writer` helpers; `McpServerStartError`
- src/ccr/claude/mcp_relay.py — (new, CCR-025) ~35-LOC stdio↔Unix-socket relay subprocess spawned by claude per `--mcp-config`
- src/ccr/claude/process.py — `ClaudeProcess`: idempotent `start()`/`stop()` with SIGTERM+SIGKILL grace, stderr ring buffer (8192 bytes), line-buffered async generator, `send_user_turn()` writer; public read-only `pid` property; `send_permission_response()` removed (CCR-024); `mcp_argv: Sequence[str] | None` parameter added (CCR-025) for argv splicing; argv builder reads `permission_mode`/`disallowed_tools`/`allowed_tools` from settings and slots flags between resume/continue and MCP block (CCR-029)
- src/ccr/claude/log.py — `JsonlSessionLog` with `append()`, `read_from(seq)`, `tail()` (asyncio.Event-notified); module-level `prune()` for retention-count and age-based cleanup
- src/ccr/claude/manager.py — `SessionManager`: single asyncio lock enforcing one-session-at-a-time invariant, lifecycle FSM, log-before-publish ordering, synthetic crash event on abnormal exit; `info()` async method returning `{session_id, pid, started_at, status}`; error classes `SessionError`, `NoActiveSessionError`, `StaleSessionError`; permission-gating dicts/accessors/helpers/`send_permission` removed (CCR-024); `McpPermissionServer` instance, lazy start, `resolve_permission()`/`is_permission_pending()` public API, `cancel_pending` in teardown, `shutdown()` method added (CCR-025)
- src/ccr/config.py — `mcp_permission_timeout_seconds: int = 120` with validator 1..3600 added (CCR-025); `permission_mode: Literal[...] | None`, `allowed_tools: list[str]`, `disallowed_tools: list[str]` settings with comma-CSV env parser and mutual-exclusivity validator added (CCR-029)
- src/ccr/bot/formatting.py — `event_to_messages()` returning `OutboundMessage = tuple[str, InlineKeyboardMarkup | None]`; `_PendingKeyboard` sentinel re-introduced (CCR-025); `_format_mcp_permission()` branch added for `McpPermissionRequest` envelopes
- src/ccr/bot/handlers/permission.py — `cb_permission` body restored with real handler (CCR-025): `_ALLOWED_CHOICES` whitelist, `resolve_permission` call, message edit with username suffix; `_parse_callback` helper and `router` registration preserved
- src/ccr/bot/keyboards.py — `permission_kb` keyboard widget reused as-is for MCP permission requests (CCR-025)
- src/ccr/server.py — `_broadcast_loop` fans out `McpPermissionRequest` envelopes via `_materialise_keyboards`; `manager.shutdown()` called in finally (CCR-025)
- tests/fakes/__init__.py — package marker
- tests/fakes/fake_claude.py — env-var-driven JSONL emitter replacing the real claude binary in tests
- tests/fakes/fake_claude — POSIX shell shim (0o755) that invokes fake_claude.py
- tests/test_event_bus.py — 8 tests: pub/sub, slow-consumer WARN, weakref GC, topic isolation, cancel safety
- tests/test_claude_events.py — 12 tests: variant round-trips, JSON schema non-empty, parse-error fallback, schema-drift fallback, extra=allow round-trip
- tests/test_claude_log.py — 13 tests: sequential seq, resume, in-order read, EOF boundary, tail, concurrent tailers, cancel safety, prune (retention-count, age-based, malformed line, idempotent open, parent-dir creation, missing-dir prune)
- tests/test_session_manager.py — lifecycle, crash detection, idempotent stop, new_session-while-running, send-without-session, first_prompt truncation, last_event_at debounce; + 5 CCR-025 cases (resolve_permission happy path, unknown id, teardown cancels, idempotent MCP start)
- tests/test_mcp_tool.py — 15 tests (13 from CCR-025 + 2 from CCR-028): in-memory MCP client driver exercising Future resolution, timeout/deny, cancel_pending, concurrent calls, double-resolve, argv property; + `test_bridge_unix_socket_roundtrip` (real Unix-socket transport round-trip) + `test_socket_listener_chmods_socket_to_owner_only` (0o700 chmod assertion)
- tests/test_claude_process.py — (new, CCR-025) 3 tests: mcp_argv splicing into argv, user extra_args still appended last; extended (CCR-029) with 4 argv tests: permission-mode flag position, disallowed-tools flag, allowed-tools flag, defaults-unchanged regression
- tests/test_bot_permission.py — 16 cases (CCR-025 extended): real cb_permission handler, forged choice, stale id, concurrent taps, edit failure, HTML escape
- tests/test_broadcast.py — + 1 CCR-025 case: McpPermissionRequest fan-out with keyboard materialisation
- tests/test_config.py — Settings validation tests; extended (CCR-029) with 5 new cases: permission_mode literal acceptance, invalid literal rejection, mutual-exclusivity validator, allowed_tools CSV parse, defaults produce no argv change
- .env.example — environment variable template; extended (CCR-029) to document MCP_PERMISSION_TIMEOUT_SECONDS, PERMISSION_MODE, ALLOWED_TOOLS, DISALLOWED_TOOLS with recommended starting values

## Relations
- depends on: core
- used by: chat-bot (CCR-008, CCR-009, CCR-010), web-viewer (CCR-012, CCR-013, CCR-015)

## Change history
- [CCR-007]: implemented EventBus, ClaudeEvent discriminated-union schema, ClaudeProcess subprocess wrapper, JsonlSessionLog JSONL logger, SessionManager single-session enforcer, and full fake-claude test harness
- [CCR-018]: added ResultUsage sub-model + optional usage field on ResultEvent; added ClaudeProcess.pid property; added SessionManager.info() snapshot method with started_at tracking
- [CCR-009]: added permission gating to SessionManager — four gating dicts, is_telegram_paused/wait_for_resume/is_permission_choice_valid accessors, _record_pending_permission/_clear_pending_permission helpers, teardown clears gate state and wakes orphaned resume Events; process.py TODO(CCR-009) updated to TODO(CCR-019)
- [CCR-024]: removed dead CCR-009 permission-gating code (`PermissionRequest` event variant, `send_permission_response` writer, `SessionManager` gating dicts/accessors/helpers + `send_permission`, `_PendingKeyboard` sentinel, `_broadcast_loop` pause/buffer/drain, `_materialise_keyboards`/`_flush_buffer`); kept `keyboards.permission_kb`, `handlers.permission._parse_callback` + router registration, and `OutboundMessage` tuple shape dormant for CCR-025; `cb_permission` body stubbed to raise `NotImplementedError`
- [CCR-025]: implemented MCP permission-prompt-tool integration — new `McpPermissionServer` (mcp.py) + `mcp_relay.py` relay subprocess; `McpPermissionRequest` synthetic bus envelope (events.py); `mcp_argv` parameter on `ClaudeProcess` (process.py); `SessionManager` wired with lazy MCP start, `resolve_permission`/`is_permission_pending`/`shutdown` (manager.py); `_PendingKeyboard` + `_materialise_keyboards` re-introduced (formatting.py, server.py); real `cb_permission` handler restored (handlers/permission.py); `mcp>=1.0,<2` added; NOTE: `_handle_relay_connection` is a non-functional stub — actual socket→MCP-Server AnyIO bridge deferred to CCR-028; socket chmod(0o700) also deferred to CCR-028
- [CCR-028]: implemented relay socket → MCP `Server.run` bridge in `_handle_relay_connection` (mcp.py): frames newline-delimited JSON-RPC bytes into `SessionMessage` objects via module-level `_socket_reader`/`_socket_writer` helpers, runs three concurrent tasks under `anyio.create_task_group`; added `os.chmod(sock_path, 0o700)` immediately after socket creation; removed `pragma: no cover` stub; added Unix-socket integration test and chmod assertion test (test_mcp_tool.py); 245 tests at 87.42% coverage
- [CCR-029]: added `permission_mode` / `allowed_tools` / `disallowed_tools` settings to `config.py` (Literal validation, comma-CSV env parser via `Annotated[list[str], NoDecode]` + field_validator, mutual-exclusivity model_validator); `ClaudeProcess.start()` argv builder slots the three flags between resume/continue and MCP block; `SessionManager` passes settings into every ClaudeProcess; `.env.example` documents the new keys; 24 new tests across test_claude_process.py and test_config.py; 257 tests at 87.40% coverage
